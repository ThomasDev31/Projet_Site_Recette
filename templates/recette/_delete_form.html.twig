<form method="post" action="{{ path('recette.deleteId', {'id': recette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette.id) }}">
    <button class="btn btn-lg btn-primary">Delete</button>
</form>
